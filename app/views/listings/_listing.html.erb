 <tr>
    <td width= "240" height= "100"><%= image_tag listing.image(:thumbnail) %></td>
    <td width= "70" height= "100"><%= listing.location %></td>
    <td width= "100" height= "100"><%= listing.vehicle %></td>
    <td width= "360" height= "100"><%= listing.description %></td>
    <td width= "80" height= "100">£<%= listing.rate %> / hour</td>
    
    <td><%= link_to 'Show', listing %></td>

    <% if current_user == listing.user %>
    <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
    <td><%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
 	<% end %>

  </tr>